<form method="post" action="{{ path('tennis_utilisateur_delete', {'id': tennis_utilisateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tennis_utilisateur.id) }}">
    <br />
    <button class="btn btn-primary">Supprimer mon compte</button>
</form>
